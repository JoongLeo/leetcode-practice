name: Sync LeetCode Submissions

on:
  # 每天北京时间 23:00 自动运行
  schedule:
    - cron: '0 15 * * *'  # UTC 15:00 = 北京时间 23:00
  
  # 手动触发
  workflow_dispatch:
  
  # 每次 push 到 main 分支时触发（用于测试）
  push:
    branches: [ main ]

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout 代码
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 获取完整历史，便于 git 操作
    
    - name: 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install requests
    
    - name: 同步 LeetCode 提交
      env:
        LEETCODE_CN_SESSION: ${{ secrets.LEETCODE_CN_SESSION }}
        LEETCODE_CN_CSRF_TOKEN: ${{ secrets.LEETCODE_CN_CSRF_TOKEN }}
      run: |
        python sync.py
    
    - name: 提交更改
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add .
        git diff --staged --quiet || git commit -m "🤖 自动同步 LeetCode 提交 $(date +'%Y-%m-%d %H:%M:%S')"
    
    - name: 推送到 GitHub
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}
